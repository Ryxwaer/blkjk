services:
  blackjack:
    build: .
    image: blackjack:latest
    pull_policy: build
    container_name: blackjack
    networks:
      - nginx-proxy-manager_default
    restart: unless-stopped

networks:
  nginx-proxy-manager_default:
    external: true
